# ButterNovel 移动端 App 开发计划

> 短篇小说阅读与创作平台 - Google Play & App Store

**版本**: 3.0
**更新日期**: 2025-12-23
**目标**: 打造抖音式短篇小说体验

---

## 目录

1. [产品定位](#产品定位)
2. [核心功能](#核心功能)
3. [技术方案](#技术方案)
4. [开发阶段](#开发阶段)
5. [API 设计](#api-设计)
6. [上架准备](#上架准备)

---

## 产品定位

### 目标用户

- 喜欢碎片化阅读的年轻用户
- 想要分享自己故事的创作者
- 全球英语用户群体

### 核心特点

| 特点 | 说明 |
|------|------|
| 只做短篇 | 5,000-20,000 字的完整故事 |
| 抖音式推荐 | For You 垂直滑动浏览 |
| 人人可创作 | 一个账号 = 读者 + 作者 |
| 社交属性 | 关注、点赞、评论、分享 |

### 与 Web 端差异

| Web 端 | 移动端 |
|--------|--------|
| 长篇 + 短篇 | 只做短篇 |
| 章节制 | 完整故事 |
| 管理员上传 | 用户自主创作 |
| 传统列表浏览 | 抖音式滑动推荐 |

---

## 核心功能

### 底部导航 (5 Tabs)

```
┌─────────┬─────────┬─────────┬─────────┬─────────┐
│ For You │Following│   ➕    │Bookshelf│ Profile │
└─────────┴─────────┴─────────┴─────────┴─────────┘
```

### Tab 1: For You (推荐)

**交互设计**:
- 全屏卡片式浏览
- 上滑下一个，下滑上一个
- 右侧操作栏（❤️ 💬 📚 ↗️）
- 点击卡片进入阅读器
- 顶部搜索入口
- 下拉刷新

**推荐算法**:
```
用户兴趣标签     30%
阅读历史相似     25%
点赞收藏相关     20%
热门趋势         15%
新发布内容       10%
```

### Tab 2: Following (关注)

**功能**:
- 关注作者的最新发布
- 时间线形式展示
- 未读红点提醒
- 点击直接进入阅读

**UI 布局**:
```
┌─────────────────────────────────┐
│ 👤 Author Name       2h ago    │
│ 「Story Title」                 │
│ 5,000 words · Romance          │
│ ❤️ 89  💬 12                   │
└─────────────────────────────────┘
```

### Tab 3: Create (创作) ➕

**功能**:
- 创建新短篇
- 管理已创作作品
- 草稿箱
- 作品数据统计

**创作流程**:
```
1. 填写标题
2. 选择分类/标签
3. 上传封面（可选）
4. 编写内容
5. 预览
6. 发布/存草稿
```

**编辑器功能**:
- 富文本编辑（加粗、斜体、引用）
- 字数统计
- 自动保存
- 预览模式

### Tab 4: Bookshelf (书架)

**功能**:
- 收藏的故事
- 阅读历史
- 阅读进度
- 继续阅读

**分类**:
- All (全部)
- Favorites (收藏)
- History (历史)
- Downloads (下载 - 可选)

### Tab 5: Profile (我的)

**功能**:
- 个人资料编辑
- 阅读统计
- 创作统计
- 设置入口
- 通知中心
- 登出

**统计数据**:
```
读者统计: 读了多少本 / 多少字
作者统计: 发布了多少本 / 获得多少赞
```

### 阅读器

**短篇专用设计**:
```
┌─────────────────────────────────┐
│ ← Back          Story Title    │
├─────────────────────────────────┤
│                                 │
│     [沉浸式阅读内容区域]         │
│                                 │
│     手势：                       │
│     - 点击中间：显示/隐藏菜单    │
│     - 左右翻页（可选）           │
│     - 长按：复制/标注            │
│                                 │
├─────────────────────────────────┤
│ Aa   🌙   📖   💬              │
│ 字体  夜间  进度  评论          │
└─────────────────────────────────┘
```

**设置选项**:
- 字体大小：Small / Medium / Large / Extra Large
- 背景色：白色 / 米色 / 灰色 / 黑色
- 行间距：紧凑 / 标准 / 宽松
- 夜间模式

### 登录注册

**支持方式**:
- Google 登录 ⭐
- Apple 登录 ⭐ (iOS 必须)
- Email + Password
- 游客模式（有限功能）

**游客限制**:
- ✅ 浏览 For You
- ✅ 阅读故事
- ❌ 点赞/收藏
- ❌ 评论
- ❌ 创作
- ❌ 关注作者

### 其他功能

**搜索**:
- 标题搜索
- 作者搜索
- 标签搜索
- 搜索历史
- 热门搜索

**通知**:
- 关注作者发布新作品
- 收到评论回复
- 作品被点赞/收藏
- 系统公告

**分享**:
- 系统分享面板
- 复制链接
- 分享卡片图片

**举报**:
- 不当内容
- 抄袭/侵权
- 垃圾信息

---

## 技术方案

### 技术栈

```json
{
  "框架": "Expo SDK 52+",
  "核心": "React Native 0.76+",
  "路由": "Expo Router",
  "状态": "@tanstack/react-query + zustand",
  "表单": "react-hook-form + zod",
  "UI": "NativeWind (Tailwind CSS)",
  "存储": "expo-secure-store + async-storage",
  "认证": "expo-auth-session",
  "推送": "expo-notifications + FCM",
  "图片": "expo-image + expo-image-picker"
}
```

### 项目结构

```
mobile/
├── app/                      # Expo Router 页面
│   ├── _layout.tsx           # 根布局
│   ├── (tabs)/               # Tab 导航
│   │   ├── _layout.tsx       # Tab 布局
│   │   ├── index.tsx         # For You
│   │   ├── following.tsx     # Following
│   │   ├── create.tsx        # Create
│   │   ├── bookshelf.tsx     # Bookshelf
│   │   └── profile.tsx       # Profile
│   ├── auth/
│   │   ├── login.tsx
│   │   └── register.tsx
│   ├── reader/
│   │   └── [id].tsx          # 阅读器
│   ├── story/
│   │   └── [id].tsx          # 故事详情
│   ├── author/
│   │   └── [id].tsx          # 作者主页
│   ├── create/
│   │   ├── new.tsx           # 新建故事
│   │   └── [id]/edit.tsx     # 编辑故事
│   ├── settings/
│   │   ├── index.tsx
│   │   ├── profile.tsx
│   │   └── notifications.tsx
│   └── search.tsx
│
├── components/
│   ├── ui/                   # 基础 UI 组件
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Card.tsx
│   │   ├── Modal.tsx
│   │   └── Loading.tsx
│   ├── story/                # 故事相关组件
│   │   ├── StoryCard.tsx
│   │   ├── StoryList.tsx
│   │   └── ActionBar.tsx
│   ├── reader/               # 阅读器组件
│   │   ├── ReaderView.tsx
│   │   └── ReaderSettings.tsx
│   └── create/               # 创作组件
│       ├── Editor.tsx
│       ├── CoverPicker.tsx
│       └── TagSelector.tsx
│
├── lib/
│   ├── api/
│   │   ├── client.ts         # API 客户端
│   │   ├── auth.ts           # 认证 API
│   │   ├── stories.ts        # 故事 API
│   │   └── user.ts           # 用户 API
│   └── utils/
│       ├── format.ts         # 格式化工具
│       └── storage.ts        # 本地存储
│
├── hooks/
│   ├── useAuth.ts
│   ├── useStories.ts
│   └── useReader.ts
│
├── stores/
│   ├── authStore.ts          # 认证状态
│   ├── readerStore.ts        # 阅读器设置
│   └── createStore.ts        # 创作状态
│
├── assets/
│   ├── images/
│   └── fonts/
│
├── app.json
├── eas.json
└── package.json
```

### 复用 Web 端代码

**可直接复用**:
- API 端点（后端）
- Zod 验证逻辑
- 格式化工具函数
- TypeScript 类型定义

**需要重写**:
- 所有 UI 组件
- 样式（Tailwind → NativeWind）
- 导航逻辑
- 存储逻辑

---

## 开发阶段

### Phase 1: 基础框架 (2周)

```
目标: 可运行的 App 骨架 + 登录功能

├── Expo 项目初始化
├── 导航架构搭建
├── 基础 UI 组件库
├── API 客户端
├── Google 登录
├── Apple 登录
└── 用户状态管理
```

**关键文件**:
- `app/_layout.tsx`
- `app/(tabs)/_layout.tsx`
- `lib/api/client.ts`
- `stores/authStore.ts`
- `hooks/useAuth.ts`

### Phase 2: For You 推荐 (2周) ⭐

```
目标: 核心推荐浏览体验

├── 推荐 API 开发
├── 垂直滑动浏览组件
├── 故事卡片组件
├── 点赞/收藏交互
├── 分享功能
└── 搜索入口
```

**核心组件**:
- `app/(tabs)/index.tsx`
- `components/story/StoryCard.tsx`
- `components/story/ActionBar.tsx`

### Phase 3: 阅读器 (1周)

```
目标: 沉浸式阅读体验

├── 阅读器页面
├── 阅读设置面板
├── 进度保存
├── 夜间模式
└── 评论入口
```

**核心组件**:
- `app/reader/[id].tsx`
- `components/reader/ReaderView.tsx`
- `components/reader/ReaderSettings.tsx`
- `stores/readerStore.ts`

### Phase 4: 创作功能 (2周) ⭐

```
目标: 完整的创作流程

├── 创作页面 UI
├── 富文本编辑器
├── 封面上传
├── 标签选择
├── 草稿保存
├── 发布流程
└── 作品管理
```

**核心组件**:
- `app/(tabs)/create.tsx`
- `app/create/new.tsx`
- `components/create/Editor.tsx`
- `components/create/CoverPicker.tsx`

### Phase 5: 关注系统 (1周)

```
目标: 社交关注功能

├── 关注/取关 API
├── 关注页面 UI
├── 更新时间线
├── 未读标记
└── 作者主页
```

**核心组件**:
- `app/(tabs)/following.tsx`
- `app/author/[id].tsx`

### Phase 6: 书架 & 个人中心 (1周)

```
目标: 用户个人空间

├── 书架页面
├── 收藏列表
├── 阅读历史
├── 个人资料编辑
├── 设置页面
└── 通知中心
```

### Phase 7: 优化 & 上架 (2周)

```
目标: 发布到应用商店

├── 性能优化
├── Bug 修复
├── 应用图标 & 启动画面
├── 商店截图
├── 隐私政策
├── 审核提交
└── 上架发布
```

---

## API 设计

### 移动端专用 API

```typescript
// For You 推荐
GET /api/mobile/for-you
Query: { page, limit, tags? }
Response: { stories: Story[], nextPage: number }

// 关注动态
GET /api/mobile/following-updates
Query: { page, limit }
Response: { updates: Update[], nextPage: number }

// 创建故事
POST /api/mobile/stories
Body: {
  title: string,
  content: string,
  coverImage?: string,
  tags: string[],
  categoryId: number,
  isDraft: boolean
}

// 获取我的作品
GET /api/mobile/my-stories
Query: { page, limit, status?: 'published' | 'draft' }

// 编辑故事
PUT /api/mobile/stories/:id
Body: { title?, content?, coverImage?, tags?, isDraft? }

// 删除故事
DELETE /api/mobile/stories/:id

// 发布故事
POST /api/mobile/stories/:id/publish
```

### 复用现有 API

```typescript
// 收藏
POST /api/library
DELETE /api/library

// 关注
POST /api/user/follow
DELETE /api/user/follow

// 评分
POST /api/novels/:id/rate

// 评论
POST /api/paragraph-comments
GET /api/paragraph-comments

// 搜索
GET /api/search

// 个人资料
GET /api/profile
PUT /api/profile

// 通知
GET /api/notifications
POST /api/notifications/:id/read
```

---

## 上架准备

### Google Play

| 要求 | 说明 |
|------|------|
| 开发者账号 | $25 一次性费用 |
| 隐私政策 | 必须提供 URL |
| 内容分级 | 填写问卷 |
| 截图 | 至少 2 张 |
| 功能图片 | 1024 x 500 |
| 应用图标 | 512 x 512 |
| App Bundle | .aab 格式 |

### App Store

| 要求 | 说明 |
|------|------|
| 开发者账号 | $99/年 |
| Sign in with Apple | **必须实现** |
| 隐私政策 | 必须提供 URL |
| App Privacy | 数据收集声明 |
| 截图 | 多尺寸 (6.7", 6.5", 5.5") |
| 应用图标 | 1024 x 1024 无透明 |
| 年龄分级 | 内容问卷 |

### 审核注意事项

**常见拒绝原因**:
1. 缺少 Sign in with Apple (iOS)
2. 隐私政策不完整
3. 功能不完整或崩溃
4. 截图与实际不符
5. 缺少内容举报机制

**建议**:
- 充分测试后再提交
- 准备完整的审核说明
- 首次提交留出 1-2 周审核时间
- 准备应对审核反馈的修改

---

## 快速启动

```bash
# 1. 创建 Expo 项目
npx create-expo-app@latest mobile --template blank-typescript
cd mobile

# 2. 安装依赖
npx expo install @react-navigation/native @react-navigation/bottom-tabs
npx expo install react-native-screens react-native-safe-area-context
npm install @tanstack/react-query zustand
npm install react-hook-form @hookform/resolvers zod
npx expo install expo-secure-store @react-native-async-storage/async-storage
npx expo install expo-auth-session expo-web-browser
npm install nativewind tailwindcss
npx expo install expo-image expo-image-picker
npx expo install expo-notifications

# 3. 配置 EAS
eas build:configure

# 4. 启动开发
npx expo start
```

---

**让短篇阅读触手可及，让创作人人可为** 📱
